export function AuthPrompt() {
  return (
    <div className="auth-prompt">
      <div className="auth-prompt-content">
        <h2>Welcome to GitHub Release Tracker</h2>
        <p>Please log in with your GitHub account to start tracking repositories.</p>
        <p className="auth-prompt-subtitle">
          Login to personalize your repository tracking experience.
        </p>
      </div>
    </div>
  );
}

